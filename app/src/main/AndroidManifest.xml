<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.cropbit.sahathanahomecare">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/HomeTheme">
        <activity android:name=".activity.SignUpActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity
            android:name=".activity.HomeActivity"
            android:theme="@style/HomeTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAjmKN4bjjwKDTia-iL79XPendKYGx5RYc" />

        <activity
            android:name=".activity.ListActivity"
            android:label="@string/requests_title"/>
        <activity android:name=".activity.RequestActivity" android:label="@string/new_title" />
        <activity android:name=".activity.SentActivity" android:theme="@style/HomeTheme.NoActionBar"></activity>

        <activity
            android:name="com.vexigon.libraries.onboarding.ui.activity.UserBenefitsActivity"
            android:theme="@style/HomeTheme.NoActionBar" />
    </application>

</manifest>